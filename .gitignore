# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

secret.yml
secret.yaml

/oteldb
/otelproxy
/otelfaker
/chotel
/odbbackup
/odbrestore
/odbmigrate
/odbagent
/bin

vmagent-remotewrite-data

# goreleaser
dist
# The admin frontend's built SPA is committed so `go:embed` works without a Bun
# toolchain (re-include it after the broad `dist` rule above).
!/internal/adminhandler/frontend/dist
!/internal/adminhandler/frontend/dist/**
coverage.txt
.task

/requests.rwq
/report.yml
/otelbench

.gemini/
gha-creds-*.json

# references
/_ref
