/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * oteldb admin panel API
 * Administrative API for an oteldb instance: instance info and effective configuration, per-signal component health, ClickHouse storage statistics, and runtime controls.

 * OpenAPI spec version: 0.1.0
 */

/**
 * One part column's physical description and the size it takes on the backend.
 */
export interface PartColumn {
  /** Column name. */
  name: string;
  /** Physical type of the column's values. */
  kind: string;
  /** Value codec the column is encoded with. */
  codec: string;
  /** Block-compression algorithm applied on top of the codec. */
  compress: string;
  /** Block-compression level the column was written at; 0 means the algorithm default or an uncompressed column. Merged metric parts climb a size-graduated ladder, so it varies from part to part.
 */
  level?: number;
  /** Size of the column's object; 0 for a constant-collapsed column, which has none. */
  bytes: number;
}
