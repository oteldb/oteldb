/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * oteldb admin panel API
 * Administrative API for an oteldb instance: instance info and effective configuration, per-signal component health, ClickHouse storage statistics, and runtime controls.

 * OpenAPI spec version: 0.1.0
 */

/**
 * Cumulative erasure-coding activity of this node, present only when the cluster runs with a private (per-node) backend. All zeros when no tenant has an EC policy. A high reconstruct rate on cold reads is expected; growing errors are not.

 */
export interface ECStats {
  /** Cold parts erasure-coded by this node as their compaction owner. */
  converted: number;
  /** Failed conversion attempts (retried on a later maintenance tick). */
  convert_errors: number;
  /** Shard slots rebuilt from surviving shards. */
  repaired_slots: number;
  /** Failed repair attempts. */
  repair_errors: number;
  /** Parts whose staged foreign shards were pruned after distribution. */
  pruned_staged_parts: number;
  /** Read-path object reconstructions (no local full copy). */
  reconstructs: number;
  /** Reconstructions that failed and surfaced to the query. */
  reconstruct_errors: number;
}
