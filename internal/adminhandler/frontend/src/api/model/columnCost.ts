/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * oteldb admin panel API
 * Administrative API for an oteldb instance: instance info and effective configuration, per-signal component health, ClickHouse storage statistics, and runtime controls.

 * OpenAPI spec version: 0.1.0
 */

/**
 * One column's share of a stream cost.
 */
export interface ColumnCost {
  name: string;
  raw_bytes: number;
  /** Approximate, apportioned as StreamCost.disk_bytes. */
  disk_bytes: number;
  /** Estimated distinct values in this column (HyperLogLog, ~1.6% standard error). 0 for an int column and for a group outside the sketch budget.
 */
  distinct: number;
  /** Distinct over values with every run of ASCII digits collapsed to '#'. A large distinct with a tiny distinct_normalized means the source is emitting one templated line with an embedded timestamp or id instead of structured fields — a fixable diagnosis.
 */
  distinct_normalized: number;
}
