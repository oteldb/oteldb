/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * oteldb admin panel API
 * Administrative API for an oteldb instance: instance info and effective configuration, per-signal component health, ClickHouse storage statistics, and runtime controls.

 * OpenAPI spec version: 0.1.0
 */

/**
 * Cumulative shared-nothing part-mirroring activity of this node, present only when the cluster runs with a private (per-node) backend.

 */
export interface PartSyncStats {
  /** Sync attempts including no-ops (the sync-loop liveness probe). */
  passes: number;
  /** Passes that installed a newer peer copy. */
  mirrored: number;
  /** Objects fetched from peers. */
  copied: number;
  /** Total size of objects fetched from peers. */
  copied_bytes: number;
  /** Stale local objects deleted after the quarantine delay. */
  pruned: number;
  /** Passes that failed part-way (retried next maintenance tick). */
  errors: number;
  /** When the last mirroring pass completed (the replication-staleness probe). */
  last_sync?: string;
}
